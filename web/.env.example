# The contact form (/contact) uses Netlify Forms, so it needs NO client-side
# key or third-party service. On Netlify, submissions are captured automatically
# and are emailed to your inbox once you add an email notification under
# Forms -> Notifications (see docs/DEPLOY.md).
#
# There are currently no build-time environment variables required for the web app.
